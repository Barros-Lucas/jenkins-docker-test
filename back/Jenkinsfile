pipeline {
  agent any
  tools {
    maven 'M3'
  }
  stages {
    stage('Build image') {
      steps {
        	sh  'cd back/ && mvn package'
		sh 'mkdir -p target/dependency'
	      	sh 'cd target/dependency/ && ls && jar -xf ../*jar'
		
		dockerfile {
			filename 'dashboard/back-test-jenkins'
			dir 'back'
		}
      }
    }
  }
}
