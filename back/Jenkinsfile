pipeline {
  agent any
  tools {
    maven 'M3'
  }
  stages {
    stage('Build image') {
      steps {
        	sh  'cd back/ && mvn package'
	      	su 'ls'
		sh 'mkdir -p back/target/dependency && ls && (cd back/target/dependency; jar -xf ../*jar)'
		
		dockerfile {
			filename 'dashboard/back-test-jenkins'
			dir 'back'
		}
      }
    }
  }
}
